<!--
código antigo para logar
<div>
      <h2>Página ------LOGIN-----</h2>

      <input
        type='text'
        value={email}
        onChange={handleEmailInput}
        placeholder='Digite seu e-mail'
      />
      <input
        type='password'
        value={password}
        onChange={handlePasswordInput}
        placeholder='Digite sua senha'
      />
      <button onClick={handleLogin}>Logar</button>
</div> -->
